@model List<string>

<script>
    function filterMachine(value) {
      window.location.href = getUrlWithAppendedParameter('machine', value);
    }
</script>

<div class="dropdown">
    <div class="dropdown-trigger">
        <button class="button is-info is-rounded" aria-haspopup="true" aria-controls="dropdown-menu">
            <p id="filter-by-machine-text">Filter by Machine <i class="arrow down"></i></p>
            @* <span class="icon is-small"> *@
            @*   <i class="fas fa-angle-down" aria-hidden="true"></i> *@
            @* </span> *@
        </button>
    </div>
    <div class="dropdown-menu has-text-centered" id="environment-dropdown-menu" role="menu">
        <div class="dropdown-content has-text-centered">
            @foreach (var machineName in Model)
            {
                <a class="dropdown-item">
                    <button class="button is-fullwidth is-rounded" onclick="filterMachine('@machineName');">
                        @machineName
                    </button>
                </a>
            }
        </div>
    </div>
</div>